# Retired checkout metadata

The active project is the parent folder, C:/NUS-ISS.
Run npm start there. This directory only retains protected Git/test metadata;
its application files were moved into .archive/before-consolidation for recovery.
Do not run, commit or push from this retired checkout.
